//Deletes row from array
export function delTaskRow(indx) {
    for(let task of taskArray){
        console.log(`taskProj is ${task.proj}`)
    }
    // taskArray.splice(indx, 1) 
    // populateTaskList(taskArray, pname)
}
